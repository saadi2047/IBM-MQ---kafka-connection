# Check current connect image in DEV
oc get kafkaconnect dev-kafka-connect-mq -n dev-kafka -o yaml | grep "image:"

# Also check from the running pod
oc describe pod dev-kafka-connect-mq-connect-0 -n dev-kafka | grep "Image:"

# Check what's in the DEV values.yaml for connect image
helm get values epay-common -n dev-epay-common | grep -A5 "kafkaConnect"

