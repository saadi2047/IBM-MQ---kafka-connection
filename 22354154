oc get pods --all-namespaces -o wide | grep worker2.dev | grep -v "Completed\|Terminating"

oc adm top pod --all-namespaces | grep worker2.dev 2>/dev/null | sort -k3 -rh | head -20

# Check all pods on worker2.dev
oc get pods --all-namespaces -o wide | grep worker2.dev.sbiepay.sbi | grep -v Completed

# Check top memory consuming pods on worker2.dev
oc adm top pod --all-namespaces --sort-by=memory | grep worker2.dev 2>/dev/null

# Check node description - allocated resources
oc describe node worker2.dev.sbiepay.sbi | grep -A20 "Allocated resources"

# Check node conditions - any memory pressure
oc describe node worker2.dev.sbiepay.sbi | grep -A5 "Conditions"
