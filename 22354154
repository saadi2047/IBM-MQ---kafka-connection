# Check if SIT uses same registry or different
oc get secret -n sit-kafka | grep pull

# Check what registry SIT currently uses
helm get values epay-common -n sit-epay-common | grep -iE "imageRegistry|imagePull|registry"
