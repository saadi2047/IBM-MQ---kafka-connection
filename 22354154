oc get kafkaconnect dev-kafka-connect-mq -n dev-kafka -o yaml | grep -A3 "imagePullSecrets"

# Check if the image can be pulled using jfrog secret
oc get secret jfrog-pull-secret -n sit-kafka -o jsonpath='{.data.\.dockerconfigjson}' | \
  base64 -d | python3 -m json.tool | grep -i "registry\|server\|auth" | head -10

# Check if microservices pull secret exists in sit-epay-common
oc get secret -n sit-epay-common | grep -iE "pull|microservices|jfrog"

