# Check what jfrog-pull-secret contains (which registry it authenticates to)
oc get secret jfrog-pull-secret -n sit-kafka -o yaml | grep -i "auths\|registry" | head -5

# Check if microservices-sit-pull-secret exists in sit-kafka
oc get secret -n sit-kafka | grep microservices

# Check what pull secret the DEV kafka connect pods use
oc get kafkaconnect dev-kafka-connect-mq -n dev-kafka -o yaml | grep -i "imagePullSecret\|pullSecret"

# Test from SIT connect pod once deployed
nc -vz 10.176.10.213 2001  # NEFT
nc -vz 10.176.10.213 2005  # RTGS
