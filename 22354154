# Check actual labels on broker pods
oc get pod dev-kafka-cluster-broker-0 -n dev-kafka --show-labels

# Check if the label strimzi.io/name exists
oc get pods -n dev-kafka -l strimzi.io/name=dev-kafka-cluster-broker

# Check what labels broker pods actually have
oc get pod dev-kafka-cluster-broker-0 -n dev-kafka -o yaml | grep -A20 "labels:"

