oc get pods -n sit-kafka -o wide | grep broker

oc exec sit-kafka-cluster-broker-0 -n sit-kafka -- \
  bin/kafka-topics.sh \
  --bootstrap-server localhost:9092 \
  --list | grep -iE "neft|rtgs|mis"

oc get pods -n sit-kafka | grep connect
oc get kafkaconnect -n sit-kafka

oc get kafkaconnector -n sit-kafka

helm get values epay-common -n sit-epay-common | grep -A20 "kafkaConnect"
