# Check if podAntiAffinity is present in the broker node pool
oc get kafkanodepool broker -n dev-kafka -o yaml | grep -A20 "affinity"

# Check the Helm deployed values
helm get values epay-common -n dev-epay-common | grep -A15 "affinity"

# Check if the KafkaNodePool has the anti-affinity configured
oc get kafkanodepool -n dev-kafka
oc get kafkanodepool broker -n dev-kafka -o yaml | grep -A30 "template"
