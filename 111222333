First — confirm MQ reachability from inside a Connect pod (this is the source failure):
oc exec dev-kafka-connect-mq-connect-1 -n dev-kafka -- bash -c "timeout 5 bash -c '</dev/tcp/10.176.10.213/2001' && echo NEFT-2001-OPEN || echo NEFT-2001-FAIL"
oc exec dev-kafka-connect-mq-connect-1 -n dev-kafka -- bash -c "timeout 5 bash -c '</dev/tcp/10.176.10.213/2005' && echo RTGS-2005-OPEN || echo RTGS-2005-FAIL"
If these FAIL, the connectors can't recover no matter what you do — it's MQ-side or the egress firewall/route from the dev nodes to 10.176.10.213. Check whether the MQ listener is up and whether anything changed on the firewall path overnight.
Second — confirm the broker admin path. CC and Connect both time out creating topics against node 1 and node 2. Check broker health and whether a broker is wedged:
oc get pods -n dev-kafka -o wide -l strimzi.io/cluster=dev-kafka-cluster
oc exec dev-kafka-cluster-broker-0 -n dev-kafka -- bin/kafka-broker-api-versions.sh --bootstrap-server localhost:9092 2>&1 | head
oc logs dev-kafka-cluster-broker-1 -n dev-kafka --tail=80 | grep -iE "error|isr|shrink|timeout|not enough"
oc get kafka dev-kafka-cluster -n dev-kafka -o jsonpath='{.status.conditions}{"\n"}'
Brokers all recreated Jun 18, 6:08–6:10 AM (~11h ago) — so something rolled them this morning. Check the operator:
oc logs -n dev-kafka -l name=strimzi-cluster-operator --since=14h | grep -iE "roll|broker|reconcil|error" | tail -40
Once MQ is reachable again, the source connectors recover on their own (the task retries). If they don't auto-recover after MQ is back, restart them:
oc annotate kafkaconnector neft-mq-source rtgs-mq-source rtgs-mq-sink strimzi.io/restart=true -n dev-kafka --overwrite
