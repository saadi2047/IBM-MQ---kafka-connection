The one command that would prove the root cause is:

oc get pods -A | grep strimzi-cluster-operator

then

oc logs <strimzi-cluster-operator-pod> -n <operator-namespace> --since=24h

You tried:

oc logs deployment/strimzi-cluster-operator -n <operator-namespace>
