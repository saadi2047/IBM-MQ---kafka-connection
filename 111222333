# Confirm mirror maker is disabled/gone in int-kafka
oc get pods -n int-kafka | grep mirror-maker

# Check operator logs - should no longer see the timeout error
oc logs -n openshift-operators amq-streams-cluster-operator-v3.0.1-2-98c8d5f6-z4tmn --since=10m | grep -iE "int-kafka|mirror-maker|timeout|error" | tail -20


