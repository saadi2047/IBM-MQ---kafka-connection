oc exec dev-kafka-cluster-broker-0 -n dev-kafka -- \
  bin/kafka-consumer-groups.sh \
  --bootstrap-server localhost:9092 \
  --describe --all-groups 2>/dev/null | grep neft.inbound

# Check neft-mq-source connector logs
oc logs dev-kafka-connect-mq-connect-0 -n dev-kafka --tail=50 | grep -iE "neft|message|error|poll|fetch"

oc exec dev-kafka-cluster-broker-0 -n dev-kafka -- \
  bin/kafka-console-consumer.sh \
  --bootstrap-server localhost:9092 \
  --topic neft.inbound.kafka.topic \
  --from-beginning \
  --max-messages 60


oc exec dev-kafka-connect-mq-connect-0 -n dev-kafka -- bash -c \
  "timeout 5 bash -c '</dev/tcp/10.176.10.213/2001' && echo OPEN || echo FAIL"
