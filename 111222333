# Find exact parent path of livenessProbe in the CRD
oc get crd kafkaconnects.kafka.strimzi.io -o yaml | grep -B20 "livenessProbe:" | grep -E "^\s+\w+:|livenessProbe" | head -20

# Also check what's directly under connectContainer
oc get crd kafkaconnects.kafka.strimzi.io -o yaml | grep -A30 "connectContainer:" | head -35

