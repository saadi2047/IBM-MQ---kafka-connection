# Are the restarts still accumulating, or did they stop after the blip?
oc get pods -n dev-kafka -l strimzi.io/cluster=dev-kafka-connect-mq

# What's the broker health / why did broker-2 drop?
oc get pods -n dev-kafka | grep broker
oc describe pod dev-kafka-cluster-broker-2 -n dev-kafka | grep -A5 "Last State\|Events" | tail -20
