# Check connect-0 previous crash reason
oc describe pod dev-kafka-connect-mq-connect-0 -n dev-kafka | grep -A5 "Last State"

# Check if OOMKilled or other reason
oc describe pod dev-kafka-connect-mq-connect-0 -n dev-kafka | grep -A10 "State\|Last State\|Exit Code\|Reason"

# Check events around that time
oc get events -n dev-kafka --sort-by='.lastTimestamp' | tail -30


# Watch restart counts over time
oc get pods -n dev-kafka -w | grep connect
