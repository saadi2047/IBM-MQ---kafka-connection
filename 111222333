# Check external route for Kafka bootstrap
oc get route -n dev-kafka | grep kafka

# Check the external service/route for port 9094 (external listener)
oc get kafka dev-kafka-cluster -n dev-kafka -o jsonpath='{.status.listeners}' | python3 -m json.tool
