oc get kafkaconnect dev-kafka-connect-mq -n dev-kafka -o yaml | grep -A5 "ownerReferences\|operator"

# Check which operator is watching dev-kafka namespace
oc get subscription -A | grep amq
oc get csv -A | grep amq

# Check the actual CRD stored version
oc get crd kafkaconnects.kafka.strimzi.io -o jsonpath='{.spec.versions[*].name}'

# Most importantly - see what livenessProbe path actually is in the CRD
oc get crd kafkaconnects.kafka.strimzi.io -o yaml | grep -B10 "livenessProbe:" | head -40
